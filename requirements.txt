# Core
anthropic>=0.40.0
APScheduler>=3.10.4
PyYAML>=6.0.2
python-dotenv>=1.0.1
requests>=2.32.3

# Optional integrations (install as you wire each one live).
# The system runs without these — adapters fall back to demo mode.
# stripe>=11.0.0
# google-api-python-client>=2.150.0
# google-auth>=2.35.0
# google-auth-oauthlib>=1.2.1
